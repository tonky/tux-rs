# 1.1 — Workspace + Build Infrastructure

**Phase:** 1 — Project Scaffolding
**Estimate:** 1 day (6 hours)
**Depends on:** Nothing (first sub-phase)
**Unlocks:** All subsequent phases

## Goal

Set up the Cargo workspace with compilable skeletons for all 3 Rust crates, build tooling, and CI. No real logic — just infrastructure that compiles and passes linting.

## Deliverables

### Cargo Workspace

```
tux-rs/
├── Cargo.toml           # Workspace: members = ["tux-core", "tux-daemon", "tux-tui"]
├── Cargo.lock
├── Justfile
├── .gitignore
├── README.md
├── tux-core/
│   ├── Cargo.toml       # lib crate
│   └── src/lib.rs
├── tux-daemon/
│   ├── Cargo.toml       # bin crate
│   └── src/main.rs
├── tux-tui/
│   ├── Cargo.toml       # bin crate
│   └── src/main.rs
└── tux-kmod/            # C kernel shims (not a Rust crate)
    └── README.md
```

### Dependencies (pinned)

**tux-core** (lib):
- `serde` + `serde_derive` 1.x
- `toml` 0.8+
- `thiserror` 2.x

**tux-daemon** (bin):
- `tux-core` (path)
- `tokio` 1.x (features: full)
- `zbus` 5.x (features: tokio)
- `nix` 0.29+ (features: ioctl, fs, uio)
- `tracing` + `tracing-subscriber` 0.3+
- `sd-notify` 0.5+
- `serde` 1.x
- `toml` 0.8+
- `anyhow` 1.x

**tux-tui** (bin):
- `tux-core` (path)
- `ratatui` 0.30+
- `crossterm` 0.29+ (features: event-stream)
- `tokio` 1.x (features: full)
- `zbus` 5.x (features: tokio)
- `serde` 1.x
- `color-eyre` 0.6+

### Justfile Recipes

```just
build            # cargo build --workspace
test             # cargo test --workspace
clippy           # cargo clippy --workspace -- -D warnings
fmt              # cargo fmt --all -- --check
fmt-fix          # cargo fmt --all
run-daemon       # cargo run -p tux-daemon
run-tui          # cargo run -p tux-tui
install-daemon   # cargo install --path tux-daemon
install-tui      # cargo install --path tux-tui
```

### CI (GitHub Actions)

`.github/workflows/ci.yml`:
- Trigger: push + PR to main
- Matrix: stable Rust
- Steps: fmt check, clippy, test
- Cache: cargo registry + target dir

### Skeleton Code

**tux-core/src/lib.rs:**
```rust
//! tux-core: Hardware model system for TUXEDO laptops.
pub fn version() -> &'static str {
    env!("CARGO_PKG_VERSION")
}
```

**tux-daemon/src/main.rs:**
```rust
fn main() {
    println!("tux-daemon v{}", tux_core::version());
}
```

**tux-tui/src/main.rs:**
```rust
fn main() {
    println!("tux-tui v{}", tux_core::version());
}
```

## Verification

- [ ] `just build` compiles without errors
- [ ] `just test` passes (trivial tests)
- [ ] `just clippy` passes with zero warnings
- [ ] `just fmt` passes (no formatting changes)
- [ ] Each crate can reference `tux-core`

## Reference

- tuxedo-drivers-rs: `Cargo.toml` workspace layout
- tcc-rs: `Cargo.toml` dependency versions (tokio 1.51, zbus 5.14, ratatui 0.30, crossterm 0.29)
