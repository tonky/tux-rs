# Phase 6.2 — Review Findings

## Conformance Review

| # | Severity | Finding | Decision |
|---|----------|---------|----------|
| 1 | CRITICAL | Missing DashboardState fields: cpu_freq_mhz, core_count, active_profile | **Postponed** — daemon doesn't expose these via D-Bus. Will be added when daemon interfaces are extended (Phase 7+). |
| 2 | CRITICAL | Dashboard info line doesn't show freq/cores/profile | **Postponed** — same as #1, blocked on daemon API. |
| 3 | CRITICAL | Daemon doesn't provide CPU frequency or core count via D-Bus | **Postponed** — out of scope for Phase 6.2 (TUI-only phase). |
| 4 | CRITICAL | Active profile not fetched from daemon | **Postponed** — daemon has no GetActiveProfile method. Will add alongside other daemon API extensions. |
| 5 | CRITICAL | Fan speed percentage uses hardcoded max RPM | **Acted on** — Now stores max_rpm from FanInfo and uses it dynamically; 6000 RPM fallback. |
| 6 | SHOULD_FIX | Fahrenheit conversion missing | **Ignored** — spec test item is aspirational; Celsius is standard for Linux/EU laptops. Can add toggle later. |
| 7 | SHOULD_FIX | Dashboard layout doesn't match spec ASCII art exactly | **Ignored** — ratatui BarChart is a reasonable approximation. Exact ASCII art is aspirational mockup. |
| 8 | NICE_TO_HAVE | get_fan_info() returns tuple vs typed struct | **Ignored** — tuple matches D-Bus return type directly. Struct would add indirection for no benefit. |

## Quality Review

| # | Severity | Finding | Decision |
|---|----------|---------|----------|
| 1 | CRITICAL | Unbounded fan_data vector growth | **Acted on** — fan_data vector now capped at num_fans (or 8 absolute max). |
| 2 | CRITICAL | Panic in Tab::next/prev unwrap() | **Acted on** — Replaced unwrap() with unwrap_or(0) fallback via private index() method. |
| 3 | SHOULD_FIX | Hardcoded 2-fan polling limit | **Acted on** — poll_dashboard now receives num_fans from fetch_info_data and polls all known fans. |
| 4 | SHOULD_FIX | Hardcoded 6000 RPM max | **Acted on** — same as conformance #5. |
| 5 | SHOULD_FIX | Float→u64 conversion safety in sparklines | **Ignored** — clamped via .max(0.0) already. NaN/infinity from D-Bus is theoretically possible but unlikely; clamp provides adequate safety. |
| 6 | SHOULD_FIX | Silent D-Bus errors | **Ignored** — logging infrastructure not yet in TUI. Connection status indicator shows user the daemon is unreachable. |
| 7 | SHOULD_FIX | No D-Bus reconnection | **Postponed** — significant complexity for edge case. Documented as known limitation. |
