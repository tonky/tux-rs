# Phase 6.5 Review Findings — Remaining Tabs

## Conformance Review
**Verdict: 88% → 95% after fixes**

All 6 tabs implemented with form fields matching spec. All 14 D-Bus methods present. Module structure matches spec.

### Gaps Addressed ✅
1. **Charging capability-driven disabling**: Added `model.charging.supported = false` when `charging_thresholds = false` in Capabilities TOML.
2. **Keyboard capability-driven disabling**: Added `model.keyboard.supported = false` when `keyboard_type = "None"` in Capabilities TOML.
3. **Webcam device switching key hint mismatch**: Fixed hint to show `[Shift+←][Shift+→]` matching actual Shift+arrow keybinding.

### Gaps Deferred
4. **Keyboard mode options adapt to device capabilities**: Static mode list. Dynamic filtering requires device capability data not yet available. Defer to Phase 7.1 (ITE Keyboard Backlight).
5. **Render-without-crash tests for settings/keyboard/charging/display**: These are simple form renders; the Form widget is well-tested. Low risk.

## Quality Review Findings

### CRITICAL — Acted Upon ✅
1. **`FormSaved` didn't call `mark_saved()` on the form**: Fixed — every tab arm now calls `form.mark_saved()` before setting status message. Test added.
2. **Webcam key hints showed wrong binding**: Fixed — changed from `[←][→]` to `[Shift+←][Shift+→]`.

### IMPROVEMENT — Acted Upon ✅
3. **Phantom "Camera 1" in default webcam state**: Changed default to `devices: Vec::new()`.
4. **Unused `_client` parameter in `execute_form_save`**: Removed.

### IMPROVEMENT — Postponed
5. **View code duplication across 4 tab views**: Valid but ~30 lines each. Refactoring to a shared `render_form_tab` function could be done in Phase 8 polish if desired. Low urgency.
6. **Charging threshold cross-field validation**: Start > End is an invalid config. Defer to Phase 7.2 (Charging Thresholds) when the daemon validates this.
7. **Text fields non-editable**: `FieldType::Text` fields can't be typed into (no character capture). Acceptable for now; text fields are mostly display (KB Color, Profile Name). Will address if needed in Phase 7.
8. **`FormSaveError` routes to current tab, not originating tab**: Minor UX issue on tab switch during async save. Low probability. Defer.

## Test Coverage
- 2 new capability-disabling tests + enhanced FormSaved test
- 292 total tests (71 + 118 + 103)
- All pass, clean clippy
