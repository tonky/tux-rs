# Phase 7.1 Review Findings

## Conformance Review (92%)

All core requirements met. Two gaps: udev hotplug monitoring (not implemented), test coverage (~85%).

## Quality Review — Findings & Decisions

### CRITICAL

| # | Finding | Decision | Status | Reasoning |
|---|---------|----------|--------|-----------|
| 1 | `set_row_col()` silently ignores out-of-bounds coordinates | **IGNORE** | — | Intentional design — hardware doesn't support bounds error, masking prevents UB. Consistent with C driver. |
| 2 | Mode changed while device OFF — stale state on turn_on | **NON-ISSUE** | ✅ | All 4 drivers already call self.flush() at end of turn_on() — stale state is already applied. |
| 3 | Missing zone bounds validation in D-Bus set_color() | **ACT** | ✅ | Added zone < zone_count check in D-Bus wrapper with InvalidInput error. |

### IMPORTANT

| # | Finding | Decision | Status | Reasoning |
|---|---------|----------|--------|-----------|
| 4 | Duplicated brightness scaling logic across 3 drivers | **POSTPONE** | — | Works correctly; dedup is polish. Phase 8. |
| 5 | Inconsistent zone handling across drivers | **IGNORE** | — | By design — zone semantics are device-specific. D-Bus get_keyboard_info() already reports zone_count per device. |
| 6 | `available_modes()` allocates Vec<String> every call | **POSTPONE** | — | Low frequency call; optimize in Phase 8 if profiling shows need. |
| 7 | Inconsistent mode support/naming across drivers | **IGNORE** | — | Expected — different hardware has different modes. `available_modes()` + `set_mode()` validates at runtime. |
| 8 | D-Bus set_brightness keyboard_index validation | **IGNORE** | — | Already handled by `with_keyboard()` returning proper error. |
| 9 | Inadequate discovery test coverage | **POSTPONE** | — | Discovery requires real /dev/hidraw or complex mocking. Phase 8 integration tests. |
| 10 | ioctl error reporting is generic | **POSTPONE** | — | Nice to have; add context in Phase 8 polish. |

### IMPROVEMENT

| # | Finding | Decision | Status | Reasoning |
|---|---------|----------|--------|-----------|
| 11 | Unnecessary write_control() wrappers | **IGNORE** | — | Improves readability; single-line but semantically named. |
| 12 | Overly complex LbVariant matching | **IGNORE** | — | Current code is clear; table pattern is marginal improvement. |
| 13 | Color scaling edge case test coverage | **POSTPONE** | — | Scaling clamps to 0–255; edge cases unlikely to matter. |
| 14 | D-Bus error messages lack keyboard index | **POSTPONE** | — | Polish item for Phase 8. |
| 15 | Inconsistent default brightness initialization | **IGNORE** | — | All start at MAX_BRIGHTNESS; device-specific maxima are correct. |
| 16 | Integration test with mock HID devices | **POSTPONE** | — | Phase 8 integration test harness. |

### CONFORMANCE GAPS

| # | Finding | Decision | Status | Reasoning |
|---|---------|----------|--------|-----------|
| U1 | Udev hotplug monitoring not implemented | **POSTPONE** | — | Spec requirement but explicitly deferred. Discovery at startup is functional. Phase 8 or future work. |
