# Phase 7.2 Review Findings

## Conformance Review (86%)

All core requirements met. Main gap: D-Bus tests only cover Clevo, not Uniwill.

## Quality Review — Findings & Decisions

### CRITICAL

| # | Finding | Decision | Status |
|---|---------|----------|--------|
| 1 | get_charging_settings uses `.ok()` on thresholds, silently losing I/O errors | **ACT** | ✅ DONE — errors now propagated; backend returns 0 for unsupported (omitted from TOML) |
| 2 | Inconsistent unsupported-op handling: Clevo returns `Ok(None)`, Uniwill returns `Err(Unsupported)` | **ACT** | ✅ DONE — Uniwill now returns `Ok(0)` / `Ok(())` for unsupported ops |

### IMPORTANT

| # | Finding | Decision | Status |
|---|---------|----------|--------|
| 3 | `get_charge_profile()` uses `unwrap_or(None).unwrap_or_default()` — swallows I/O errors | **ACT** | ✅ DONE — now uses `.map(opt.unwrap_or_default()).map_err(...)` |
| 4 | Missing D-Bus + Uniwill test coverage | **ACT** | ✅ DONE — added 5 Uniwill D-Bus tests (settings, profile, priority, invalid inputs, thresholds) |

### IMPROVEMENT

| # | Finding | Decision | Reasoning |
|---|---------|----------|-----------|
| 5 | Clevo clamps silently without warning | **IGNORE** | Clamping is standard for hardware backends; D-Bus already constrains to u8 |
| 6 | Double-option chain is unclear | **ACT** | Cleaned up as part of #1/#2 fix |
| 7 | No logging in D-Bus charging methods | **POSTPONE** | Consistent with other D-Bus interfaces; add in Phase 8 polish |
| 8 | Single trait for two architectures | **IGNORE** | Works fine; only 2 backends, not worth splitting |
| 9 | Missing error condition docs on trait | **IGNORE** | Internal trait, not a public API |
| 10 | Field order in ChargingSettings | **IGNORE** | Cosmetic, TOML order doesn't matter for callers |
