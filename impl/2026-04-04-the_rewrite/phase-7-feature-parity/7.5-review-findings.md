# Phase 7.5 — NVIDIA GPU Power Review Findings

## Conformance: 32% (strict against spec)

The spec listed GPU telemetry as PRIMARY and NB02 cTGP as SECONDARY.
Implementation focused on cTGP control with profile integration.

### Key Gaps
- GPU telemetry (GpuInfo struct, hwmon parsing) — MISSING → ACT: implement basic hwmon telemetry
- Dynamic boost control — MISSING → IGNORE: kernel driver only exposes db_offset in DEBUG mode, not available in production sysfs
- TOML-based GetPowerSettings/SetPowerSettings — MISSING → IGNORE: individual typed D-Bus methods are cleaner pattern
- Interface name GpuPower vs Power — IGNORE: GpuPower is more precise
- Type i32 vs u8 for ctgp_offset — IGNORE: EC register is u8, C driver uses u8, u8 is correct

## Quality Findings

### CRITICAL

1. No bounds checking on cTGP offset → **ACT**: The kernel driver accepts any u8 and writes raw to EC. Reasonable to keep u8 but document range. Not adding GpuPowerBounds since the kernel driver itself doesn't enforce range limits — clamping on our side could mask issues.
   - **Decision: IGNORE** — kernel driver is the authority, no artificial bounds

2. Valid range undocumented → **IGNORE** — doc comments are not in scope per implementation discipline

3. No device capability check at runtime → **IGNORE** — already handled: D-Bus interface only registered when backend exists (main.rs + dbus/mod.rs conditional registration)

### IMPORTANT

4. Inconsistent D-Bus error codes → **IGNORE** — interface only registers when backend exists, NotSupported not needed
5. Missing ProfileApplier GPU test → **ACT** ✅
6. No sysfs write verification → **POSTPONE** — matches all other backends (TDP, charging, fan)
7. D-Bus missing backend availability check → **IGNORE** — same as #3
8. Device table invariant test (only Uniwill has Nb02Nvidia) → **ACT** ✅

### IMPROVEMENT

9–14. All **POSTPONE** — performance optimization, doc comments, concurrency testing, etc.

## Actions Taken

- [x] Add GPU telemetry (GpuInfo, hwmon backend) — added gpu/hwmon.rs with discover_gpus(), GpuInfo struct, 8 tests
- [x] Add D-Bus get_gpu_info() on System interface — reads hwmon at /sys/class/hwmon
- [x] Add ProfileApplier GPU test (#5) — 2 tests added (apply + noop)
- [x] Add device table invariant test (#8) — 2 tests (gpu_power only on Uniwill, matches gpu_temp)
