# Phase 8.1 — Integration Test Harness: Review Findings

## Conformance Review

| Deliverable | Verdict |
|---|---|
| MockSysfs: create/read/write temp files for each device type | **PASS** (hidraw missing, all others covered) |
| TestDaemon: starts on session bus, responds to D-Bus calls | **PASS** |
| Full roundtrip: daemon start → profile create → sysfs verify → stop | **PASS** |
| TUI: TestBackend renders all tabs without panic | **PASS** |
| CI: GitHub Actions workflow | **PASS** |

### Gaps
1. Missing `add_hidraw(vid, pid)` — MockSysfs has no hidraw mock. Low severity. → **POSTPONED** (will add if/when keyboard integration tests needed)
2. No `buffer_contains_text` assertions in TUI tests — no-panic only. → **IGNORED** (spec example, not requirement) 
3. TestDaemon signature differs from spec (takes DetectedDevice instead of MockSysfs). → **IGNORED** (design improvement, MockFanBackend more appropriate for D-Bus-level tests)
4. No generic `dbus_call()` method — replaced by typed zbus proxies. → **IGNORED** (improvement over spec)

## Code Quality Review

### Acted Upon
- **#3 [Medium] Drop safety for TestDaemon**: Added `Drop` impl that sends shutdown signal. → **ACTED ON**
- **#10 [Low] Error-path tests**: Added 3 error-path integration tests. → **ACTED ON**

### Postponed
- **#1 [Medium] TestDaemon doesn't use MockSysfs**: Integration tests test D-Bus layer, not sysfs backends. Backend-level sysfs tests exist in unit tests. → **POSTPONED** (future phases may wire MockSysfs to TestDaemon for end-to-end sysfs verification)
- **#2 [Medium] Repeated boilerplate**: 7 tests, manageable. → **POSTPONED** (extract helper if test count grows beyond ~15)
- **#8 [Low] 15-param serve_on_bus**: Already flagged with clippy allow. → **POSTPONED** (builder pattern in future cleanup)

### Ignored
- **#4 [Low] Temperature unit convention**: Comment in test is sufficient. → **IGNORED**
- **#5 [Low] MockSysfs path traversal**: Test-only code with TempDir. → **IGNORED**
- **#6 [Low] Duplicated path strings in MockSysfs**: Test fixture stability. → **IGNORED**
- **#7 [Low] CI only runs tux-daemon integration tests**: No other crates have ignored tests. → **IGNORED** (correct as-is)
- **#9 [Low] 100ms sleep flakiness**: Acceptable for session bus tests. → **IGNORED** (revisit if CI flakes)
