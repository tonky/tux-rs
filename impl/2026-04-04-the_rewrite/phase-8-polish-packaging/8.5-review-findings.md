# 8.5 Review Findings

## Conformance Review (Agent A)
**Verdict: ✅ COMPLETE** — All 7 batches from 8.5-missing-pieces.md implemented with tests.

| Batch | Status | Notes |
|-------|--------|-------|
| 1: Daemon D-Bus gaps (CPU freq/count/profile) | ✅ | 6 new tests |
| 2: TUI Dashboard gaps | ✅ | Fields, polling, rendering |
| 3: Fn Lock support | ✅ | sysfs R/W + 3 tests |
| 4: Keyboard/Power endpoints | ✅ | KeyboardState + PowerSettings structs |
| 5: Guard display/webcam tabs | ✅ | Defaults unsupported, enabled on data |
| 6: serve_on_bus → DbusConfig | ✅ | Eliminates 15-param function |
| 7: D-Bus reconnection | ✅ | Exponential backoff 1s→30s |

No items missed or partially implemented.

## Quality Review (Agent B)

### Fix Now — ACTED ON
- **Collapsible if in system.rs**: Fixed (clippy caught this, resolved via `just check`).

### Fix Now — IGNORED (acceptable risk)
- **CPU frequency precision**: `total_khz / count` then `/1000` is fine — u64 can hold sum of 1024 cores × ~6GHz without overflow. → **IGNORED**
- **String allocation for CPU dir scan**: `to_string_lossy()` on ~16 entries during one-time init is negligible. → **IGNORED**
- **Silent keyboard hardware failures**: Matches existing pattern in daemon; hardware errors are logged elsewhere. → **IGNORED**
- **D-Bus call timeout**: zbus has internal timeouts; adding another layer adds complexity without clear benefit. → **IGNORED**
- **Connection health check threshold (3 failures)**: 3 seconds is aggressive but provides fast feedback to user. Can revisit with real-world usage. → **IGNORED**

### Improve Later — POSTPONED
- Fn Lock path should be detected at init per platform
- Input validation for PowerSettings before hardware writes
- Builder pattern for DbusConfig
- Distinguish zbus error types in client
- Document display/webcam `supported` flag pattern

### Good As-Is
- CPU frequency averaging, core counting
- Test coverage comprehensive
- Reconnection logic correct with backoff + reset
- DbusConfig eliminates clippy warning cleanly
- TEA pattern consistently applied for display/webcam guards
