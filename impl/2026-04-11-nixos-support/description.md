# NixOS Support

This feature adds native support for NixOS and the Nix package manager to `tux-rs`.
It includes a `flake.nix` that exposes packages for the daemon, TUI, and kernel modules,
along with a NixOS module for seamless system integration and an automated VM test suite.
